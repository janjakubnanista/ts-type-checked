{
  "name": "@ts-type-checked-examples/root",
  "private": true,
  "scripts": {
    "build": "yarn workspaces run build",
    "clean": "rm -rf index.js && yarn workspaces run clean",
    "start": "yarn workspaces run start"
  },
  "workspaces": ["jest", "rollup", "mocha", "ts-node", "ttypescript", "webpack"]
}
