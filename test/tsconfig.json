{
  "extends": "../tsconfig.json",
  "compilerOptions": {
    "plugins": [
      { "transform": "ts-reflection/transformer" },
      { "transform": "ts-type-checked/transformer", "logLevel": "debug", "mode": "development" },
    ]
  },
  "include": ["setups", "utils"],
  "exclude": ["node_modules"]
}
